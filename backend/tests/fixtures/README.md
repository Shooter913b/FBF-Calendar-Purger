# Calendar event fixtures

Placeholder samples based on typical Feedback Fruits title patterns. Replace with real
anonymized JSON from your institution's Canvas API when available:

```
GET /api/v1/calendar_events?type=event&context_codes[]=course_{id}&all_events=true
```

Never commit access tokens.
